#include <stdio.h>

int main(){
    // increament operator
    // ++i or i++

    int i=1;
    printf("%d \n", i++);  // Use, then increase
    printf("%d \n", i);

    printf("%d \n", ++i);  // Increase, then use
    printf("%d \n", i);

    return 0;
}