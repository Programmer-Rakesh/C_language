#include <stdio.h>

int main(){
    int age;
    printf("Entre your age : ");
    scanf("%d",  &age);

    age >= 18 ? print("adult \n") : printf("not an adult \n");
    // if(   )  {print(   )}       else{print(      )}

    return 0;
}